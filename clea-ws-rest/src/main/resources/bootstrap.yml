spring:
  application:
    name: clea-ws-rest

  cloud:
    consul:
      config:
        enabled: false
        format: YAML
        acl-token: token
      enabled: false
      host: localhost
      port: 8500
      scheme: http

    vault:
      enabled: false
      host: localhost
      port: 8200
      scheme: http
      token: token
