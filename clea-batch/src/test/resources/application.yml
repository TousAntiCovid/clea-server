spring:
  main:
    banner-mode: OFF

  batch:
    # Manage it's batch metadata tables
    #prevent job to launch on startup
    job:
      enabled: false
    jdbc:
      initialize-schema: always
  sql:
    init:
      mode: always
