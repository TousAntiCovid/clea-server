spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cleadb?ssl=false
    username: username
    password: password

  batch:
    # Manage it's batch metadata tables
    #prevent job to launch on startup
    job:
      enabled: false
    jdbc:
      initialize-schema: never
